<?xml version="1.0" encoding="utf-8"?>
<!--
  Configuración de seguridad de red — HorarioGenetico

  Release: solo HTTPS (producción en giecom.com.co usa HTTPS, sin restricciones).
  Debug:   se permite HTTP cleartext para desarrollo local (emulador / LAN).
           El manifest de debug activa esta excepción automáticamente.
-->
<network-security-config>
    <!-- Producción: solo conexiones HTTPS (comportamiento por defecto de Android 9+) -->
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>

    <!-- Desarrollo: permite HTTP hacia el emulador y redes LAN locales -->
    <debug-overrides>
        <trust-anchors>
            <certificates src="system" />
            <certificates src="user" />
        </trust-anchors>
    </debug-overrides>
</network-security-config>
